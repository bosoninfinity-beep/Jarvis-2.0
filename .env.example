# ═══════════════════════════════════════════════════════════════════════════
#  JARVIS 2.0 // CONFIGURATION TEMPLATE
#  Skopiuj jako .env i uzupelnij wartosci:  cp .env.example .env
# ═══════════════════════════════════════════════════════════════════════════

# ─── Gateway (Master) ────────────────────────────────────────────────────
JARVIS_PORT=18900                        # Port HTTP + WebSocket Gateway
JARVIS_HOST=0.0.0.0                      # Bind address (0.0.0.0 = dostepny z sieci)
JARVIS_AUTH_TOKEN=CHANGE_ME              # Token autoryzacji (min 32 znakow! wygeneruj: openssl rand -hex 32)

# ─── Infrastructure (Master) ─────────────────────────────────────────────
NATS_URL=nats://0.0.0.0:4222            # NATS message broker
NATS_TOKEN=CHANGE_ME                     # NATS auth token (wygeneruj: openssl rand -hex 16)
REDIS_URL=redis://localhost:6379         # Redis state store

# ─── NAS Storage ─────────────────────────────────────────────────────────
# Sciezka do wspoldzielonego folderu na QNAP NAS
# Na Master i Agentach musi byc ta sama sciezka (mount point)
JARVIS_NAS_MOUNT=/Volumes/JarvisNAS/jarvis

# ─── LLM Providers ──────────────────────────────────────────────────────
# Wymagany przynajmniej jeden klucz API
ANTHROPIC_API_KEY=                       # Claude (sk-ant-api...)
OPENAI_API_KEY=                          # GPT-4 (sk-...)
GOOGLE_AI_API_KEY=                       # Gemini (AI...)
OPENROUTER_API_KEY=                      # OpenRouter (sk-or-...)

# ─── Ollama (lokalne modele - darmowe) ───────────────────────────────────
# Zainstaluj: brew install ollama && ollama serve
# Sciagnij model: ollama pull llama3.2
OLLAMA_HOST=http://localhost:11434

# ─── VNC Remote Desktops ────────────────────────────────────────────────
# Adresy websockify na maszynach Alpha/Beta
VNC_ALPHA_HOST=mac-mini-alpha            # Hostname lub IP
VNC_ALPHA_PORT=6080                      # Port websockify
VNC_BETA_HOST=mac-mini-beta
VNC_BETA_PORT=6081

# ─── Dashboard ───────────────────────────────────────────────────────────
DASHBOARD_PORT=3000                      # Port Vite dev server

# ─── Agent Identity (tylko na maszynach Alpha/Beta) ─────────────────────
# JARVIS_AGENT_ID=agent-alpha            # agent-alpha | agent-beta
# JARVIS_AGENT_ROLE=dev                  # dev | marketing
# JARVIS_MACHINE_ID=mac-mini-alpha       # Hostname maszyny

# ─── Agent -> Master (tylko na maszynach Alpha/Beta) ────────────────────
# GATEWAY_URL=http://mac-mini-master:18900

# ─── Social Media APIs (tylko agent-beta / marketing) ───────────────────
# TWITTER_API_KEY=
# TWITTER_API_SECRET=
# TWITTER_ACCESS_TOKEN=
# TWITTER_ACCESS_SECRET=

# META_APP_ID=
# META_APP_SECRET=
# INSTAGRAM_ACCESS_TOKEN=
# FACEBOOK_PAGE_TOKEN=
# FACEBOOK_PAGE_ID=

# LINKEDIN_ACCESS_TOKEN=
# LINKEDIN_PERSON_ID=

# TIKTOK_ACCESS_TOKEN=

# ─── Research APIs (agent-beta) ─────────────────────────────────────────
# BRAVE_API_KEY=                         # Brave Search API
# PERPLEXITY_API_KEY=                    # Perplexity AI

# ─── Slack ─────────────────────────────────────────────────────────────
# SLACK_BOT_TOKEN=xoxb-...                  # Bot User OAuth Token
# SLACK_APP_TOKEN=xapp-...                  # App-Level Token (Socket Mode)
# SLACK_SIGNING_SECRET=                     # Signing Secret (HTTP Events mode)
# SLACK_DEFAULT_CHANNEL=#general            # Default channel for messages

# ─── Thunderbolt 5 Bridge (120 Gbps direct connect) ───────────────────
# Static IPs on 10.0.1.0/24 subnet.
# Mac Studio = 10.0.1.1, Smith = 10.0.1.2, Johny = 10.0.1.3
THUNDERBOLT_ENABLED=false
# MASTER_IP_THUNDERBOLT=10.0.1.1
# SMITH_IP_THUNDERBOLT=10.0.1.2
# JOHNY_IP_THUNDERBOLT=10.0.1.3
# NATS_URL_THUNDERBOLT=nats://10.0.1.1:4223
# VNC_ALPHA_HOST_THUNDERBOLT=10.0.1.2
# VNC_BETA_HOST_THUNDERBOLT=10.0.1.3
